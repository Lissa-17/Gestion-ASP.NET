@page
@model GestionHopital.Pages_Prescriptions.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Prescription</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @* @Html.DisplayNameFor(model => model.Prescription.prescription) *@
            Prescription
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Prescription.prescription)
        </dd>
        <dt class="col-sm-2">
            @* @Html.DisplayNameFor(model => model.Prescription.ConsultationId) *@
            Consultation
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Prescription.Consultation.Details)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Prescription.Id" class="btn btn-warning">Edit</a>
    <a asp-page="./Index" class="btn btn-secondary">Retour</a>
</div>
